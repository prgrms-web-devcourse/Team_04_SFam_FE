import styled from '@emotion/styled';

export const ReviewGroupWrapper = styled('div')({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '32px',
});
